import styled from "styled-components";

const ItemWrapper = styled.div`
  margin-bottom: 20px;
`;

const Title = styled.div`
  font-size: 1.3rem;
  color: black;
  margin: 15px 0;
  user-select: none;
`;

const MakerWrapper = styled.div`
  font-size: 1rem;
  margin: 5px 0;
`;

export { ItemWrapper, Title, MakerWrapper };
